// This module displays all foods. It should import the food.js component 
//and the foodCollection.js component.
    // 1. send fetch to getAllFoods() 
        //This is the fetch call to the API (foodCollection module holds the API)
    // 2. loop over [] foodBuilder for each item 
        //(NOTE: foodBuilder is the function in )
    //3. Append to DOM